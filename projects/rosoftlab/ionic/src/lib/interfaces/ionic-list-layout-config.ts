import { GridLayoutFormat } from '@rosoftlab/core';
export interface IonicListLayoutConfig {
    key?: string;
    primary: boolean;
    translateKey?: string
    formating?: GridLayoutFormat;
    customFormat?: string;
}
